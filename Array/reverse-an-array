#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
	int t, n, x;
	vector <int> v;
	cin >> t;
	while (t --){
	    cin >> n;
	    v.clear();
	    for (int i = 0; i < n; i ++){
	        cin >> x;
	        v.push_back(x);
	    }
	    for (int i = n-1; i >= 0; i --){
	        cout << v[i] << " ";
	    }
	    cout << endl;
	}
	return 0;
}
